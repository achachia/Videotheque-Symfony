{% extends 'base.html.twig' %}

{% block title %}Hello FilmsController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

 

    <h2>Listing-film</h2>
    <a href="{{ path('film_add') }}">Ajouter un film</a>
    <table BORDER=1>

      <head>

            <tr>

                  <td>ID</td>

                  <td>NOM</td>

                  <td>ACTION</td>
            </tr>

           

                 {% for film in films %}
                    <tr>
                         <td><a href="{{ path('film_show',{'id': film.id}) }}">{{ film.id }}</a></td>                   
                         <td><a href="{{ path('film_show',{'id': film.id}) }}">{{ film.title }}</a></td>
                         <td><a href="{{ path('film_remove',{'id': film.id}) }}">Supprimer la fiche</a>|<a href="{{ path('film_edit',{'id': film.id}) }}">Editer la fiche</a></td>
                    </tr>
                 {% endfor %}  

          

      </head>


    </table>
    <ul>
       
    </ul>
</div>
{% endblock %}
